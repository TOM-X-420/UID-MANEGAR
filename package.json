{
  "name": "uid-manegar-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm run typecheck && pnpm --filter @workspace/db run build && pnpm --filter @workspace/api-zod run build && pnpm --filter @workspace/api-client-react run build && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/uid-manager run build",
    "typecheck": "tsc --build --emitDeclarationOnly && pnpm --filter @workspace/uid-manager run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.0-beta"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  }
}
