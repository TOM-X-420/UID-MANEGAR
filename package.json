{
  "name": "artifacts-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r run build",
    "typecheck": "tsc --build --emitDeclarationOnly"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=9"
  }
}
